# Datto Menu

This site shows 2 menus on a blank page.

The site also shows the same menus below in an activated state.

To build the src files, run gulp. After the files are compiled, run the start script in npm and access the via the url that shows in the terminal.

To test the site, run npm test. This starts a mocha script under the test/ folder.